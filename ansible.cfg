# So `ansible-playbook ...` from the repo root finds the Proxmox inventory.
# Override paths in proxmox/ansible/ansible.cfg when running playbooks from proxmox/ansible/.
[defaults]
inventory = proxmox/ansible/inventory/hosts.yaml
host_key_checking = False
