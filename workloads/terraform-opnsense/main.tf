# Intentionally empty: add resources here or copy from examples/ once the firewall API is reachable.
#
# Routing/Gateway and some HA features are not covered by the provider yet — manage via
# Ansible, config.xml import, or GUI and document drift policy (see docs/GITOPS.md).
