# Not used by Terraform for the OPNsense VM.
#
# OPNsense on Proxmox should be installed from the official ISO (or supported image)
# and configured via the text/serial installer — not Linux-style cloud-init.
#
# If you need unattended hints, use vendor docs or a post-install config restore from backup.
