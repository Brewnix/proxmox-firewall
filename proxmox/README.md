# Proxmox (hypervisor) automation

- **[ansible/](ansible/)** — Proxmox VE host: repositories, `/etc/network/interfaces` bridges, golden LXC template, OPNsense ISO sync.
- **[terraform/](terraform/)** — Optional **node-only** Terraform state; guests belong in [workloads/terraform](../workloads/terraform/).

See the repo [README.md](../README.md) for the full two-root model and apply order.
